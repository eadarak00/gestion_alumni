services:

  # CONFIG SERVER
  ms-config:
    build: ./backend/ms-config
    container_name: ms-config
    ports:
      - "8888:8888"
    networks:
      - microservices-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8888/actuator/health"]
      interval: 10s
      timeout: 5s
      retries: 5
 
  # DISCOVERY SERVER (Eureka)
  ms-discover:
    build: ./backend/ms-discover
    container_name: ms-discover
    ports:
      - "8761:8761"
    networks:
      - microservices-net
    depends_on:
      ms-config:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8761/actuator/health"]
      interval: 10s
      timeout: 5s
      retries: 5
 
  # API GATEWAY
  ms-gateway:
    build: ./backend/ms-gateway
    container_name: ms-gateway
    ports:
      - "8088:8088"
    networks:
      - microservices-net
    depends_on:
      ms-config:
        condition: service_healthy
      ms-discover:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8088/actuator/health"]
      interval: 10s
      timeout: 5s
      retries: 5

  # MS-USER Database
  ms-user-db:
    image: mariadb:11
    container_name: ms-user-db
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: db_user
      MYSQL_USER: user
      MYSQL_PASSWORD: passer123
    ports:
      - "3308:3306"
    networks:
      - microservices-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 10s
      timeout: 30s  # Augmenter le timeout
      retries: 20
      start_period: 60s
    command: 
      - --wait-timeout=28800
      - --interactive-timeout=28800

  # MS-USER
  ms-user:
    build: ./backend/ms-user
    container_name: ms-user
    ports:
      - "8081:8081"
    networks:
      - microservices-net
    depends_on:
      ms-discover:
        condition: service_healthy
      ms-user-db:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8081/actuator/health"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 40s

  # MS-CV Database
  ms-cv-db:
    image: mariadb:11
    container_name: ms-cv-db
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: db_cv
      MYSQL_USER: cvuser
      MYSQL_PASSWORD: cvpass123
    ports:
      - "3309:3306"
    networks:
      - microservices-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "cvuser", "-p cvpass123"]
      interval: 10s
      timeout: 30s
      retries: 20
      start_period: 60s

  # MS-CV
  ms-cv:
    build: ./backend/ms-cv
    container_name: ms-cv
    ports:
      - "8082:8082"
    networks:
      - microservices-net
    depends_on:
      ms-discover:
        condition: service_healthy
      ms-cv-db:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8082/actuator/health"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 40s

  
  # FRONTEND REACT + VITE 
  frontend:
    build: ./frontend
    container_name: frontend
    ports:
      - "3000:80"         # NGINX sert l’app sur 80
    networks:
      - microservices-net
    depends_on:
      ms-gateway:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost"]
      interval: 10s
      timeout: 5s
      retries: 5

  # phpMyAdmin
  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    container_name: phpmyadmin
    environment:
      PMA_HOST: ms-user-db
      PMA_PORT: 3306
    ports:
      - "8080:80"
    networks:
      - microservices-net
    depends_on:
      - ms-user-db
    restart: unless-stopped

networks:
  microservices-net:
    driver: bridge
